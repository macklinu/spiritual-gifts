{
  "name": "spiritual-gifts",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^15.6.1",
    "react-dom": "^15.6.1",
    "react-scripts": "1.0.10"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "prettier": "prettier --no-semi --single-quote --trailing-comma=es5 --jsx-bracket-same-line 'src/**/*.js' --write"
  },
  "devDependencies": {
    "prettier": "^1.5.2"
  }
}
